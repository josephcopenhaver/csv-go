{{- define "appendRec0"}}{{if .Memclear}}w.appendRec({{else}}w.recordBuf = append(w.recordBuf, {{end}}{{end}}
{{define "appendRec1"}}{{if .Memclear}}){{else}}...){{end}}{{end -}}
{{define "appendRecJoin"}}{{if .Memclear}}, {{else}}...)
w.recordBuf = append(w.recordBuf, {{end}}{{end -}}
{{define "loadQF"}}w.loadQF_memclearO{{if .Memclear}}n{{else}}ff{{end}}_escapeO{{if .Escape}}n{{else}}ff{{end}}{{end}}
{{define "loadQFCheckUTF8"}}w.loadQF_wCheckUTF8_memclearO{{if .Memclear}}n{{else}}ff{{end}}_escapeO{{if .Escape}}n{{else}}ff{{end}}{{end}}

{{- if .ForceQuoteCommentStart -}}
func (w *Writer) writeRowAfterHeader_memclearO{{if .Memclear}}n{{else}}ff{{end}}_escapeO{{if .Escape}}n{{else}}ff{{end}}_checkUTF8O{{if .CheckUTF8}}n{{else}}ff{{end}}_controlRuneOverlapO{{if .ControlRuneOverlap}}n{{else}}ff{{end}}(comment rune) writeRowFunc {
commentBytes := []byte(string(comment))

return func(fields []FieldWriter) (int, error) {
{{- else -}}
func (w *Writer) writeRow_memclearO{{if .Memclear}}n{{else}}ff{{end}}_escapeO{{if .Escape}}n{{else}}ff{{end}}_checkUTF8O{{if .CheckUTF8}}n{{else}}ff{{end}}_controlRuneOverlapO{{if .ControlRuneOverlap}}n{{else}}ff{{end}}(fields []FieldWriter) (int, error) {
{{- end}}
	defer func() {
		w.recordBuf = w.recordBuf[:0]
	}()
	{{if .ForceQuoteCommentStart -}}

	w.writeRow = w.writeRow_memclearO{{if .Memclear}}n{{else}}ff{{end}}_escapeO{{if .Escape}}n{{else}}ff{{end}}_checkUTF8O{{if .CheckUTF8}}n{{else}}ff{{end}}_controlRuneOverlapO{{if .ControlRuneOverlap}}n{{else}}ff{{end}}
	{{end}}

	// write the first field
	{
		f := &fields[0]

		{{if .CheckUTF8 -}}
		var scanForNonUTF8 bool
		{{- end}}
		var src []byte

		switch f.kind {
		case wfkBytes:
			s := f.bytes
			if len(s) == 0 {
				if len(fields) == 1 {
					{{template "appendRec0" .}}w.twoQuotes[:w.twoQuotesByteLen]{{template "appendRecJoin" .}}w.recordSepBytes[:w.recordSepByteLen]{{template "appendRec1" .}}
					if n, err := w.writer.Write(w.recordBuf); err != nil {
						return n, writeIOErr{err}
					} else {
						return n, nil
					}
				}
				goto FIRST_FIELD_WRITTEN
			}

			{{if .CheckUTF8 -}}
			scanForNonUTF8 = (f._64_bits == 0)
			{{- end}}
			src = s
		case wfkString:
			s := f.str
			if len(s) == 0 {
				if len(fields) == 1 {
					{{template "appendRec0" .}}w.twoQuotes[:w.twoQuotesByteLen]{{template "appendRecJoin" .}}w.recordSepBytes[:w.recordSepByteLen]{{template "appendRec1" .}}
					if n, err := w.writer.Write(w.recordBuf); err != nil {
						return n, writeIOErr{err}
					} else {
						return n, nil
					}
				}
				goto FIRST_FIELD_WRITTEN
			}

			{{if .CheckUTF8 -}}
			scanForNonUTF8 = (f._64_bits == 0)
			{{- end}}
			src = unsafe.Slice(unsafe.StringData(s), len(s))
		{{if .ControlRuneOverlap -}}
		default:
			v, err := f.AppendText(w.fieldWriterBuf[:0])
			if err != nil {
				return 0, err
			}

			src = v
		{{- else -}}
		case wfkRune:
			v, err := f.AppendText(w.fieldWriterBuf[:0])
			if err != nil {
				return 0, err
			}

			src = v
		default:
			v, err := f.AppendText(w.recordBuf)
			if err != nil {
				return 0, err
			}

			{{if .Memclear}}w.setRecordBuf(v){{else}}w.recordBuf = v{{end}}
			goto FIRST_FIELD_WRITTEN
		{{- end}}
		}

		//
		// process src buf
		//

		{{if .CheckUTF8 -}}if !scanForNonUTF8 { {{- end}}
			// so just need to scan for quotes, escapes, fieldSep, CR / LF / maybe all other kinds of newline sequences / recordSep

			{{if .ForceQuoteCommentStart}}
			var i int

			if !bytes.HasPrefix(src, commentBytes) { {{end}}
				i {{if not .ForceQuoteCommentStart}}:{{end}}= bytes.IndexAny(src, w.controlRunes)
				if i == -1 {
					{{template "appendRec0" .}}src{{template "appendRec1" .}}
					goto FIRST_FIELD_WRITTEN
				}
			{{if .ForceQuoteCommentStart}}}{{end}}

			r, n := utf8.DecodeRune(src[i:])
			switch r {
			case w.quote:
				{{template "appendRec0" .}}w.quoteBytes[:w.quoteByteLen]{{template "appendRecJoin" .}}src[:i]{{template "appendRecJoin" .}}w.escapedQuote[:w.escapedQuoteByteLen]{{template "appendRec1" .}}
				{{template "loadQF" .}}(src, i+n, i+n)
			case w.escape:
				{{template "appendRec0" .}}w.quoteBytes[:w.quoteByteLen]{{template "appendRecJoin" .}}src[:i]{{template "appendRecJoin" .}}w.escapedEscape[:w.escapedEscapeByteLen]{{template "appendRec1" .}}
				{{template "loadQF" .}}(src, i+n, i+n)
			default:
				{{template "appendRec0" .}}w.quoteBytes[:w.quoteByteLen]{{template "appendRec1" .}}
				{{template "loadQF" .}}(src, 0, i+n)
			}

			{{template "appendRec0" .}}w.quoteBytes[:w.quoteByteLen]{{template "appendRec1" .}}

			goto FIRST_FIELD_WRITTEN
		{{if .CheckUTF8 -}} }

		// for each decoded rune, check if that rune fails to decode and if so then return an error
		// if the rune is in the set of controlRunes then replace whatever needs to be replaced and
		// continue loading

		{{if .ForceQuoteCommentStart -}}
		isFirstRune := true
		{{- end}}
		i := 0
		for {
			r, n := utf8.DecodeRune(src[i:])
			if n == 0 {
				// all good and no overlap issues, so just directly copy src
				{{template "appendRec0" .}}src{{template "appendRec1" .}}
				goto FIRST_FIELD_WRITTEN
			}
			if n == 1 && r == utf8.RuneError {
				return 0, ErrNonUTF8InRecord
			}

			if !strings.ContainsRune(w.controlRunes, r) {{if .ForceQuoteCommentStart}}&& (!isFirstRune || r != comment) {{end}} {
				i += n
				{{if .ForceQuoteCommentStart -}}
				isFirstRune = false
				{{- end}}
				continue
			}

			//
			// found a control rune of some kind
			//

			var err error
			switch r {
			case w.quote:
				{{template "appendRec0" .}}w.quoteBytes[:w.quoteByteLen]{{template "appendRecJoin" .}}src[:i]{{template "appendRecJoin" .}}w.escapedQuote[:w.escapedQuoteByteLen]{{template "appendRec1" .}}
				err = {{template "loadQFCheckUTF8" .}}(src, i+n, i+n)
			case w.escape:
				{{template "appendRec0" .}}w.quoteBytes[:w.quoteByteLen]{{template "appendRecJoin" .}}src[:i]{{template "appendRecJoin" .}}w.escapedEscape[:w.escapedEscapeByteLen]{{template "appendRec1" .}}
				err = {{template "loadQFCheckUTF8" .}}(src, i+n, i+n)
			default:
				{{template "appendRec0" .}}w.quoteBytes[:w.quoteByteLen]{{template "appendRec1" .}}
				err = {{template "loadQFCheckUTF8" .}}(src, 0, i+n)
			}
			if err != nil {
				return 0, err
			}

			{{template "appendRec0" .}}w.quoteBytes[:w.quoteByteLen]{{template "appendRec1" .}}

			goto FIRST_FIELD_WRITTEN
		}
		{{end}}
	}

FIRST_FIELD_WRITTEN:

	for i := 1; i < len(fields); i++ {
		{{template "appendRec0" .}}w.fieldSepBytes[:w.fieldSepByteLen]{{template "appendRec1" .}}

SUBSEQUENT_FIELD_WRITE:
		for {
			f := &fields[i]

			{{if .CheckUTF8 -}}
			var scanForNonUTF8 bool
			{{- end}}
			var src []byte

			switch f.kind {
			case wfkBytes:
				s := f.bytes
				if len(s) == 0 {
					break SUBSEQUENT_FIELD_WRITE
				}

				{{if .CheckUTF8 -}}
				scanForNonUTF8 = (f._64_bits == 0)
				{{- end}}
				src = s
			case wfkString:
				s := f.str
				if len(s) == 0 {
					break SUBSEQUENT_FIELD_WRITE
				}

				{{if .CheckUTF8 -}}
				scanForNonUTF8 = (f._64_bits == 0)
				{{- end}}
				src = unsafe.Slice(unsafe.StringData(s), len(s))
			{{if .ControlRuneOverlap -}}
			default:
				v, err := f.AppendText(w.fieldWriterBuf[:0])
				if err != nil {
					return 0, err
				}

				src = v
			{{- else -}}
			case wfkRune:
				v, err := f.AppendText(w.fieldWriterBuf[:0])
				if err != nil {
					return 0, err
				}

				src = v
			default:
				v, err := f.AppendText(w.recordBuf)
				if err != nil {
					return 0, err
				}

				{{if .Memclear}}w.setRecordBuf(v){{else}}w.recordBuf = v{{end}}
				break SUBSEQUENT_FIELD_WRITE
			{{- end}}
			}

			//
			// process src buf
			//

			{{if .CheckUTF8 -}}if !scanForNonUTF8 { {{- end}}
				// so just need to scan for quotes, escapes, fieldSep, CR / LF / maybe all other kinds of newline sequences / recordSep

				i := bytes.IndexAny(src, w.controlRunes)
				if i == -1 {
					{{template "appendRec0" .}}src{{template "appendRec1" .}}
					break
				}

				r, n := utf8.DecodeRune(src[i:])
				switch r {
				case w.quote:
					{{template "appendRec0" .}}w.quoteBytes[:w.quoteByteLen]{{template "appendRecJoin" .}}src[:i]{{template "appendRecJoin" .}}w.escapedQuote[:w.escapedQuoteByteLen]{{template "appendRec1" .}}
					{{template "loadQF" .}}(src, i+n, i+n)
				case w.escape:
					{{template "appendRec0" .}}w.quoteBytes[:w.quoteByteLen]{{template "appendRecJoin" .}}src[:i]{{template "appendRecJoin" .}}w.escapedEscape[:w.escapedEscapeByteLen]{{template "appendRec1" .}}
					{{template "loadQF" .}}(src, i+n, i+n)
				default:
					{{template "appendRec0" .}}w.quoteBytes[:w.quoteByteLen]{{template "appendRec1" .}}
					{{template "loadQF" .}}(src, 0, i+n)
				}

				{{template "appendRec0" .}}w.quoteBytes[:w.quoteByteLen]{{template "appendRec1" .}}

				break
			{{if .CheckUTF8 -}} }

			// for each decoded rune, check if that rune fails to decode and if so then return an error
			// if the rune is in the set of controlRunes then replace whatever needs to be replaced and
			// continue loading

			i := 0
			for {
				r, n := utf8.DecodeRune(src[i:])
				if n == 0 {
					// all good and no overlap issues, so just directly copy src
					{{template "appendRec0" .}}src{{template "appendRec1" .}}
					break
				}
				if n == 1 && r == utf8.RuneError {
					return 0, ErrNonUTF8InRecord
				}

				if !strings.ContainsRune(w.controlRunes, r) {
					i += n
					continue
				}

				//
				// found a control rune of some kind
				//

				var err error
				switch r {
				case w.quote:
					{{template "appendRec0" .}}w.quoteBytes[:w.quoteByteLen]{{template "appendRecJoin" .}}src[:i]{{template "appendRecJoin" .}}w.escapedQuote[:w.escapedQuoteByteLen]{{template "appendRec1" .}}
					err = {{template "loadQFCheckUTF8" .}}(src, i+n, i+n)
				case w.escape:
					{{template "appendRec0" .}}w.quoteBytes[:w.quoteByteLen]{{template "appendRecJoin" .}}src[:i]{{template "appendRecJoin" .}}w.escapedEscape[:w.escapedEscapeByteLen]{{template "appendRec1" .}}
					err = {{template "loadQFCheckUTF8" .}}(src, i+n, i+n)
				default:
					{{template "appendRec0" .}}w.quoteBytes[:w.quoteByteLen]{{template "appendRec1" .}}
					err = {{template "loadQFCheckUTF8" .}}(src, 0, i+n)
				}
				if err != nil {
					return 0, err
				}

				{{template "appendRec0" .}}w.quoteBytes[:w.quoteByteLen]{{template "appendRec1" .}}

				break
			}

			break{{end}}
		}
	}

	{{template "appendRec0" .}}w.recordSepBytes[:w.recordSepByteLen]{{template "appendRec1" .}}

	if n, err := w.writer.Write(w.recordBuf); err != nil {
		return n, writeIOErr{err}
	} else {
		return n, nil
	}
{{if .ForceQuoteCommentStart -}}
}
{{- end}}
}
