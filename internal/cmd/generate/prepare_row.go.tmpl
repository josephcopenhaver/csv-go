{{define "recBufAppend"}}{{if .ClearMemoryAfterUse}}r.appendRecBuf({{else}}r.recordBuf = append(r.recordBuf, {{end}}{{end}}
func (r *Reader) prepareRow_startOfDocO{{if .StartOfDoc}}n{{else}}ff{{end}}_memclearO{{if .ClearMemoryAfterUse}}n{{else}}ff{{end}}_errOnNLInUFO{{if .ErrOnNLInUF}}n{{else}}ff{{end}}_errOnQInUFO{{if .ErrOnQInUF}}n{{else}}ff{{end}}_1RuneRecSepO{{if .OneRuneRecSep}}n{{else}}ff{{end}}_2RuneRecSepO{{if .TwoRuneRecSep}}n{{else}}ff{{end}}_commentO{{if .Comment}}n{{else}}ff{{end}}_dropBOMO{{if .DropBOM}}n{{else}}ff{{end}}_quoteO{{if .Quote}}n{{else}}ff{{end}}_escapeO{{if .Escape}}n{{else}}ff{{end}}() bool {

	for {
		c, size, rErr := r.reader.ReadRune()
		if size > 0 && rErr != nil {
			r.setDone()
			r.ioErr(errors.Join(ErrBadReadRuneImpl, rErr))
			return false
		}

		// advance the position indicator
		r.byteIndex += uint64(size)

		if size == 1 && c == utf8.RuneError {

			//
			// handle a non UTF8 byte
			//

			{{if .StartOfDoc}}
			if rStateStartOfDoc == r.state {
				if r.errOnNoByteOrderMarker {
					r.byteIndex = 0 // special case, no BOM rune was found while at start of doc so no processed bytes were "stable"
					r.setDone()
					r.parsingErr(ErrNoByteOrderMarker)
					return false
				}

				r.state = rStateStartOfRecord
			}
			{{end}}

			if err := r.reader.UnreadRune(); err != nil {
				r.setDone()
				r.ioErr(errors.Join(ErrBadUnreadRuneImpl, err))
				return false
			}
			var b byte
			if v, err := r.reader.ReadByte(); err != nil {
				r.setDone()
				r.ioErr(errors.Join(ErrBadReadByteImpl, err))
				return false
			} else {
				b = v
			}

			switch r.state {
			case rStateStartOfRecord, rStateStartOfField:
				{{template "recBufAppend" .}}b)
				r.state = rStateInField
			case rStateInField, rStateInQuotedField:
				{{template "recBufAppend" .}}b)
				// r.state = rStateInField
			// case rStateInQuotedField:
			// 	{{template "recBufAppend" .}}b)
			// 	// r.state = rStateInQuotedField
			case rStateInQuotedFieldAfterEscape:
				r.setDone()
				r.parsingErr(errInvalidEscapeInQuotedFieldUnexpectedByte)
				return false
			case rStateEndOfQuotedField:
				r.setDone()
				r.parsingErr(ErrInvalidQuotedFieldEnding)
				return false
				// case rStateInLineComment:
				// 	// r.state = rStateInLineComment
			}

			if rErr == nil {
				continue
			}
		}
		if rErr != nil {
			r.setDone()
			if errors.Is(rErr, io.EOF) {
				return r.handleEOF()
			}
			r.ioErr(rErr)
			return false
		}

		switch r.state { {{- if .StartOfDoc}}
		case rStateStartOfDoc:
			if !isByteOrderMarker(uint32(c), size) {
				if r.errOnNoByteOrderMarker {
					r.byteIndex = 0 // special case, no BOM rune was found while at start of doc so no processed bytes were "stable"
					r.setDone()
					r.parsingErr(ErrNoByteOrderMarker)
					return false
				}
			}{{if .DropBOM}} else {
				r.state = rStateStartOfRecord
				continue
			}{{end}}

			r.state = rStateStartOfRecord
			fallthrough{{end}}
		case rStateStartOfRecord:
			{{if or .TwoRuneRecSep (not .OneRuneRecSep)}}
			if c == r.fieldSeparator {
				r.fieldLengths = append(r.fieldLengths, 0)
				// field start is unchanged because the last one was zero length
				// r.fieldStart = len(r.recordBuf)
				if r.fieldNumOverflow() {
					return false
				}
				r.state = rStateStartOfField
				r.fieldIndex++

				continue
			}

			isRecSep, immediateErr := r.isRecordSeparator(c)
			if immediateErr {
				return false
			}
			if isRecSep { {{- if not .TwoRuneRecSep}}
				//
				// TODO: set prepareRow to decided record separator handler
				//
				{{end}}r.fieldLengths = append(r.fieldLengths, 0)
				// field start is unchanged because the last one was zero length
				// r.fieldStart = len(r.recordBuf)
				// r.state = rStateStartOfRecord
				if r.checkNumFields(nil) {
					r.fieldIndex = 0
					r.recordIndex++
					return true
				}
				return false
			}

			{{if or .Quote .Comment .ErrOnNLInUF}}
			switch c {
			{{if .Quote}}case r.quote:
				r.state = rStateInQuotedField

				// not required because quote being set to \r is not allowed when record sep discovery mode is enabled
				//
				//
				// // checking if EOF was signaled from within the isRecordSeparator call before continue
				// if r.eof {
				// 	break
				// }
				continue{{end}}
			{{if .Comment}}case r.comment:
				if (!r.afterStartOfRecords || r.commentsAllowedAfterStartOfRecords) {
					r.state = rStateInLineComment

					// not required because quote being set to \r is not allowed when record sep discovery mode is enabled
					//
					//
					// // checking if EOF was signaled from within the isRecordSeparator call before continue
					// if r.eof {
					// 	break
					// }
					continue
				}
			{{end}}{{if .ErrOnNLInUF}}case '\r':
				r.setDone()
				r.parsingErr(errNewlineInUnquotedFieldCarriageReturn)
				return false
			case '\n':
				r.setDone()
				r.parsingErr(errNewlineInUnquotedFieldLineFeed)
				return false
			{{end}}
			}
			{{end}}
			{{else}}
			switch c {
			case r.fieldSeparator:
				r.fieldLengths = append(r.fieldLengths, 0)
				// field start is unchanged because the last one was zero length
				// r.fieldStart = len(r.recordBuf)
				if r.fieldNumOverflow() {
					return false
				}
				r.state = rStateStartOfField
				r.fieldIndex++

				continue
			{{if .OneRuneRecSep}}case r.recordSep[0]:
				r.fieldLengths = append(r.fieldLengths, 0)
				// field start is unchanged because the last one was zero length
				// r.fieldStart = len(r.recordBuf)
				// r.state = rStateStartOfRecord
				if r.checkNumFields(nil) {
					r.fieldIndex = 0
					r.recordIndex++
					return true
				}
				return false{{end}}
			{{if .Quote}}case r.quote:
				r.state = rStateInQuotedField

				// not required because quote being set to \r is not allowed when record sep discovery mode is enabled
				//
				//
				// // checking if EOF was signaled from within the isRecordSeparator call before continue
				// if r.eof {
				// 	break
				// }
				continue
			{{end}}{{if .Comment}}case r.comment:
				if (!r.afterStartOfRecords || r.commentsAllowedAfterStartOfRecords) {
					r.state = rStateInLineComment

					// not required because quote being set to \r is not allowed when record sep discovery mode is enabled
					//
					//
					// // checking if EOF was signaled from within the isRecordSeparator call before continue
					// if r.eof {
					// 	break
					// }
					continue
				}
			{{end}}{{if .ErrOnNLInUF}}
			case '\r':
				{{if not .OneRuneRecSep}}if isRecSep, _ := r.isRecordSeparator(c); isRecSep {
					r.fieldLengths = append(r.fieldLengths, 0)
					// field start is unchanged because the last one was zero length
					// r.fieldStart = len(r.recordBuf)
					// r.state = rStateStartOfRecord
					if r.checkNumFields(nil) {
						r.fieldIndex = 0
						r.recordIndex++
						return true
					}
					return false
				}{{end}}
				r.setDone()
				r.parsingErr(errNewlineInUnquotedFieldCarriageReturn)
				return false
			case '\n':
				{{if not .OneRuneRecSep}}if isRecSep, _ := r.isRecordSeparator(c); isRecSep {
					r.fieldLengths = append(r.fieldLengths, 0)
					// field start is unchanged because the last one was zero length
					// r.fieldStart = len(r.recordBuf)
					// r.state = rStateStartOfRecord
					if r.checkNumFields(nil) {
						r.fieldIndex = 0
						r.recordIndex++
						return true
					}
					return false
				}{{end}}
				r.setDone()
				r.parsingErr(errNewlineInUnquotedFieldLineFeed)
				return false
			{{if not .OneRuneRecSep}}default:
				if isRecSep, _ := r.isRecordSeparator(c); isRecSep {
					r.fieldLengths = append(r.fieldLengths, 0)
					// field start is unchanged because the last one was zero length
					// r.fieldStart = len(r.recordBuf)
					// r.state = rStateStartOfRecord
					if r.checkNumFields(nil) {
						r.fieldIndex = 0
						r.recordIndex++
						return true
					}
					return false
				}{{end}}
			{{end}}}
			{{end}}

			{{template "recBufAppend" .}}[]byte(string(c))...)
			r.state = rStateInField
		case rStateStartOfField:
			{{if .TwoRuneRecSep}}
			if c == r.fieldSeparator {
				r.fieldLengths = append(r.fieldLengths, 0)
				// field start is unchanged because the last one was zero length
				// r.fieldStart = len(r.recordBuf)
				if r.fieldNumOverflow() {
					return false
				}
				// r.state = rStateStartOfField
				r.fieldIndex++

				continue
			}

			isRecSep, immediateErr := r.isRecordSeparator(c)
			if immediateErr {
				return false
			}
			if isRecSep {
				r.fieldLengths = append(r.fieldLengths, 0)
				// field start is unchanged because the last one was zero length
				// r.fieldStart = len(r.recordBuf)
				r.state = rStateStartOfRecord
				if r.checkNumFields(nil) {
					r.fieldIndex = 0
					r.recordIndex++
					return true
				}
				return false
			}

			{{if or .Quote .ErrOnNLInUF}}
			switch c {
			{{if .Quote}}case r.quote:
				r.state = rStateInQuotedField

				// not required because quote being set to \r is not allowed when record sep discovery mode is enabled
				//
				//
				// // checking if EOF was signaled from within the isRecordSeparator call before continue
				// if r.eof {
				// 	break
				// }
				continue
			{{end}}{{if .ErrOnNLInUF}}case '\r':
				r.setDone()
				r.parsingErr(errNewlineInUnquotedFieldCarriageReturn)
				return false
			case '\n':
				r.setDone()
				r.parsingErr(errNewlineInUnquotedFieldLineFeed)
				return false
			{{end}}
			}
			{{end}}
			{{else}}
			switch c {
			case r.fieldSeparator:
				r.fieldLengths = append(r.fieldLengths, 0)
				// field start is unchanged because the last one was zero length
				// r.fieldStart = len(r.recordBuf)
				if r.fieldNumOverflow() {
					return false
				}
				// r.state = rStateStartOfField
				r.fieldIndex++

				continue
			{{if .OneRuneRecSep}}case r.recordSep[0]:
				r.fieldLengths = append(r.fieldLengths, 0)
				// field start is unchanged because the last one was zero length
				// r.fieldStart = len(r.recordBuf)
				r.state = rStateStartOfRecord
				if r.checkNumFields(nil) {
					r.fieldIndex = 0
					r.recordIndex++
					return true
				}
				return false{{end}}
			{{if .Quote}}case r.quote:
				r.state = rStateInQuotedField

				// not required because quote being set to \r is not allowed when record sep discovery mode is enabled
				//
				//
				// // checking if EOF was signaled from within the isRecordSeparator call before continue
				// if r.eof {
				// 	break
				// }
				continue
			{{end}}{{if .ErrOnNLInUF}}case '\r':
				{{if not .OneRuneRecSep}}if isRecSep, _ := r.isRecordSeparator(c); isRecSep {
					r.fieldLengths = append(r.fieldLengths, 0)
					// field start is unchanged because the last one was zero length
					// r.fieldStart = len(r.recordBuf)
					r.state = rStateStartOfRecord
					if r.checkNumFields(nil) {
						r.fieldIndex = 0
						r.recordIndex++
						return true
					}
					return false
				}{{end}}
				r.setDone()
				r.parsingErr(errNewlineInUnquotedFieldCarriageReturn)
				return false
			case '\n':
				{{if not .OneRuneRecSep}}if isRecSep, _ := r.isRecordSeparator(c); isRecSep {
					r.fieldLengths = append(r.fieldLengths, 0)
					// field start is unchanged because the last one was zero length
					// r.fieldStart = len(r.recordBuf)
					r.state = rStateStartOfRecord
					if r.checkNumFields(nil) {
						r.fieldIndex = 0
						r.recordIndex++
						return true
					}
					return false
				}{{end}}
				r.setDone()
				r.parsingErr(errNewlineInUnquotedFieldLineFeed)
				return false
			{{if not .OneRuneRecSep}}default:
				if isRecSep, _ := r.isRecordSeparator(c); isRecSep {
					r.fieldLengths = append(r.fieldLengths, 0)
					// field start is unchanged because the last one was zero length
					// r.fieldStart = len(r.recordBuf)
					r.state = rStateStartOfRecord
					if r.checkNumFields(nil) {
						r.fieldIndex = 0
						r.recordIndex++
						return true
					}
					return false
				}{{end}}
			{{end}}}
			{{end}}

			{{template "recBufAppend" .}}[]byte(string(c))...)
			r.state = rStateInField
		case rStateInField:
			{{if .TwoRuneRecSep}}
			if c == r.fieldSeparator {
				r.fieldLengths = append(r.fieldLengths, len(r.recordBuf)-r.fieldStart)
				r.fieldStart = len(r.recordBuf)
				if r.fieldNumOverflow() {
					return false
				}
				r.state = rStateStartOfField
				r.fieldIndex++

				continue
			}

			isRecSep, immediateErr := r.isRecordSeparator(c)
			if immediateErr {
				return false
			}
			if isRecSep {
				r.fieldLengths = append(r.fieldLengths, len(r.recordBuf)-r.fieldStart)
				r.fieldStart = len(r.recordBuf)
				r.state = rStateStartOfRecord
				if r.checkNumFields(nil) {
					r.fieldIndex = 0
					r.recordIndex++
					return true
				}
				return false
			}

			{{if or (and .Quote .ErrOnQInUF) .ErrOnNLInUF}}
			switch c {
			{{if and .Quote .ErrOnQInUF}}case r.quote:
				r.setDone()
				r.parsingErr(ErrQuoteInUnquotedField)
				return false
			{{end}}{{if .ErrOnNLInUF}}case '\r':
				r.setDone()
				r.parsingErr(errNewlineInUnquotedFieldCarriageReturn)
				return false
			case '\n':
				r.setDone()
				r.parsingErr(errNewlineInUnquotedFieldLineFeed)
				return false
			{{end}}
			}
			{{end}}
			{{else}}
			switch c {
			case r.fieldSeparator:
				r.fieldLengths = append(r.fieldLengths, len(r.recordBuf)-r.fieldStart)
				r.fieldStart = len(r.recordBuf)
				if r.fieldNumOverflow() {
					return false
				}
				r.state = rStateStartOfField
				r.fieldIndex++

				continue
			{{if .OneRuneRecSep}}case r.recordSep[0]:
				r.fieldLengths = append(r.fieldLengths, len(r.recordBuf)-r.fieldStart)
				r.fieldStart = len(r.recordBuf)
				r.state = rStateStartOfRecord
				if r.checkNumFields(nil) {
					r.fieldIndex = 0
					r.recordIndex++
					return true
				}
				return false{{end}}
			{{if and .Quote .ErrOnQInUF}}case r.quote:
				r.setDone()
				r.parsingErr(ErrQuoteInUnquotedField)
				return false
			{{end}}{{if .ErrOnNLInUF}}
			case '\r':
				{{if not .OneRuneRecSep}}if isRecSep, _ := r.isRecordSeparator(c); isRecSep {
					r.fieldLengths = append(r.fieldLengths, len(r.recordBuf)-r.fieldStart)
					r.fieldStart = len(r.recordBuf)
					r.state = rStateStartOfRecord
					if r.checkNumFields(nil) {
						r.fieldIndex = 0
						r.recordIndex++
						return true
					}
					return false
				}{{end}}
				r.setDone()
				r.parsingErr(errNewlineInUnquotedFieldCarriageReturn)
				return false
			case '\n':
				{{if not .OneRuneRecSep}}if isRecSep, _ := r.isRecordSeparator(c); isRecSep {
					r.fieldLengths = append(r.fieldLengths, len(r.recordBuf)-r.fieldStart)
					r.fieldStart = len(r.recordBuf)
					r.state = rStateStartOfRecord
					if r.checkNumFields(nil) {
						r.fieldIndex = 0
						r.recordIndex++
						return true
					}
					return false
				}{{end}}

				r.setDone()
				r.parsingErr(errNewlineInUnquotedFieldLineFeed)
				return false
			{{if not .OneRuneRecSep}}default:
				if isRecSep, _ := r.isRecordSeparator(c); isRecSep {
					r.fieldLengths = append(r.fieldLengths, len(r.recordBuf)-r.fieldStart)
					r.fieldStart = len(r.recordBuf)
					r.state = rStateStartOfRecord
					if r.checkNumFields(nil) {
						r.fieldIndex = 0
						r.recordIndex++
						return true
					}
					return false
				}{{end}}
			{{end}}}
			{{end}}

			{{template "recBufAppend" .}}[]byte(string(c))...)
			// r.state = rStateInField
		{{if .Quote}}case rStateInQuotedField:
			switch c {
			case r.quote:
				r.state = rStateEndOfQuotedField
			{{if .Escape}}case r.escape:
					r.state = rStateInQuotedFieldAfterEscape
					continue
			{{end}}default:

				{{template "recBufAppend" .}}[]byte(string(c))...)
				// r.state = rStateInQuotedField
			}
		{{if .Escape}}case rStateInQuotedFieldAfterEscape:
			switch c {
			case r.quote, r.escape:
				{{template "recBufAppend" .}}[]byte(string(c))...)
				r.state = rStateInQuotedField
			default:
				r.setDone()
				r.parsingErr(errInvalidEscapeInQuotedFieldUnexpectedRune)
				return false
			}
		{{end}}case rStateEndOfQuotedField:
			switch c {
			case r.fieldSeparator:
				r.fieldLengths = append(r.fieldLengths, len(r.recordBuf)-r.fieldStart)
				r.fieldStart = len(r.recordBuf)
				if r.fieldNumOverflow() {
					return false
				}
				r.state = rStateStartOfField
				r.fieldIndex++
			case r.quote:
				{{if .Escape}}
				r.setDone()
				r.parsingErr(ErrUnexpectedQuoteAfterField)
				return false
				{{else}}
				{{template "recBufAppend" .}}[]byte(string(r.quote))...)
				r.state = rStateInQuotedField
				{{end}}
			{{if .OneRuneRecSep}}case r.recordSep[0]:
				r.fieldLengths = append(r.fieldLengths, len(r.recordBuf)-r.fieldStart)
				r.fieldStart = len(r.recordBuf)
				r.state = rStateStartOfRecord
				if r.checkNumFields(nil) {
					r.fieldIndex = 0
					r.recordIndex++
					return true
				}
				return false
			{{end}}default:
				{{if not .OneRuneRecSep}}
				{{if .TwoRuneRecSep}}isRecSep, immediateErr := r.isRecordSeparator(c)
				if immediateErr {
					return false
				}
				if isRecSep{{else}}if isRecSep, _ := r.isRecordSeparator(c); isRecSep{{end}} {
					r.fieldLengths = append(r.fieldLengths, len(r.recordBuf)-r.fieldStart)
					r.fieldStart = len(r.recordBuf)
					r.state = rStateStartOfRecord
					if r.checkNumFields(nil) {
						r.fieldIndex = 0
						r.recordIndex++
						return true
					}
					return false
				}
				{{end}}

				r.setDone()
				r.parsingErr(ErrInvalidQuotedFieldEnding)
				return false
			}
		{{end}}{{if .Comment}}case rStateInLineComment:
			{{if .TwoRuneRecSep}}isRecSep, immediateErr := r.isRecordSeparator(c)
			if immediateErr {
				return false
			}
			if isRecSep {{- else}}
			if {{if .OneRuneRecSep}}c == r.recordSep[0]{{else}}isRecSep, _ := r.isRecordSeparator(c); isRecSep{{end}}{{end}} {
				r.state = rStateStartOfRecord
				// r.recordIndex++ // not valid in this case because the previous state was not a record
			}

			continue{{end}}
		}

		// not required because all code paths that would set this value
		// end in early returns rather than continued iterations
		//
		//
		// if r.eof {
		// 	break
		// }

		// not required because all code paths that would set this value
		// end in early returns rather than continued iterations
		//
		// these paths include calls to:
		// - nextRuneIsLF()
		// - fieldNumOverflow()
		// - checkFields()
		//
		// and every path in prepareRow() that sets `r.done = <true-expression>`
		//
		//
		// if r.done {
		// 	break
		// }

		// now, because all code paths that would call break are definitely not viable
		// there does not need to be anything after this loop all exit points are returns
	}

	// no longer required because all loop exit points are returns, no breaks
	//
	//
	// var errTrailer error
	// if r.eof {
	// 	errTrailer = io.ErrUnexpectedEOF
	// }
	// return r.checkNumFields(errTrailer)
}
